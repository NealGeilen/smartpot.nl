{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body rounded bg-primary">
            <div class="table-responsive">
                <table class="table table-striped table-hover table-light">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Username</th>
                        <th>Roles</th>
                        <th>Firstname</th>
                        <th>Lastname</th>
                        <th>Email</th>
                        <th>actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for user in users %}
                        <tr>
                            <td>{{ user.id }}</td>
                            <td>{{ user.username }}</td>
                            <td>
                                <ul class="list-group">
                                    {% for role in user.roles %}
                                        <li class="list-group-item {% if role == "ROLE_ADMIN" %}bg-gradient-primary{% else %}bg-primary{% endif %} text-white" style="padding: 8px">{{ role }}</li>
                                    {% endfor %}
                                </ul>
                            </td>
                            <td>{{ user.firstname }}</td>
                            <td>{{ user.lastname }}</td>
                            <td>{{ user.email }}</td>
                            <td>
                                <div class="btn-group">
                                    <a class="btn btn-primary" href="{{ path('user_show', {'id': user.id}) }}">show</a>
                                    <a class="btn btn-primary" href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>

            <a href="{{ path('user_new') }}">Create new</a>
        </div>
    </div>
{% endblock %}
